import styled from '@emotion/styled';

const Close = styled.p({
  margin: 0,
  padding: 0,
  color: 'red',
  position: 'absolute',
  right: '20px',
  cursor: 'pointer',
  top: '20px',
});

export default Close;
