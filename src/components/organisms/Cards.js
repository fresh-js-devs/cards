import styled from '@emotion/styled';

const Cards = styled.div({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  marginTop: '50px',
  width: '70%',
});

export default Cards;
